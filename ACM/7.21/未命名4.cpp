#include<stdio.h>
#include<iosteam>
using namespace std;
int x[201]={0},y[201]={0};
int main(){
	scnaf("%s",&x);
    scnaf("%s",&y);
    for(i=0;i<201;i++)
    x[i]=x[i]+y[i];
    while(i--){
    	if(x[i])
	}
}
